# Project maintainer settings

* docker image repository  
    * dddlab
* docker image tag
* helm chart version
* gh cli add ssh key
* gh cli add DOCKERHUB secrets

# Executor settings

* Google cloud account
* Google cloud project id
* Google cloud region
* Change password
